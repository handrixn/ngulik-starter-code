

const exec = (merchantRepository) => {
    return async (payload) => {
        // validation
        // logic
        // return

        return [{ merchantId: 1 }, { merchantId: 1 }]
    }
}

module.exports = exec;